box: wercker/nodejs
build:
  steps:
    # A step that executes `npm install` command
    - npm-install
    # A step that executes `npm test` command
    - npm-test
    # A step to install all packages from bower
    - bower-install
